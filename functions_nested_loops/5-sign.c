#include "main.h"
/**
 * print_sign - print the sign of a number ( +, -, and 0)
 *
 * Return: 1, 0, or -1
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
		printf("+");
	}
	else if (n < 0)
	{
		return (-1);
		printf("-");
	}
	else 
	{
		return (0);
		printf ("0"0;
	}
}
