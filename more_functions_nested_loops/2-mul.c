#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: integer to be multiplied
 * @b: integer to be multiplied
 * Return: Always 0
 */

int mul(int a, int b)
{
	for(a = 10; b = 19)
	{
		printf("%d\n", mul);
	}
	return (0);
}
